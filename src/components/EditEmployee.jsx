import React from 'react'

const EditEmployee = () => {
    const editId = useSelector(state => state.employee.editId);
    
    return (
        <div>EditEmployee</div>
    )
}

export default EditEmployee